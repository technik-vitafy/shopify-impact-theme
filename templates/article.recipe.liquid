<div class="recipe-container">
  Rendering
  {% assign recipe = article.metafields.recipe_data %}

  {% if recipe %}
    <div class="recipe-header">
      <h1 class="recipe-title">{{ article.title }}</h1>
      <p><strong>Difficulty:</strong> {{ recipe.difficulty | capitalize }}</p>
      <p><strong>Cooking Time:</strong> {{ recipe.cooking_time }} minutes</p>
    </div>

    <div class="recipe-picture">
      {% if recipe.picture %}
        <img src="{{ recipe.picture | image_url: 'large' }}" alt="Recipe Image for {{ article.title }}">
      {% endif %}
    </div>

    <div class="recipe-content">
      {{ recipe.content }}
    </div>
  {% else %}
    <p>No recipe data available.</p>
  {% endif %}
</div>
