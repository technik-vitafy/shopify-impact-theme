<div class="upsell-switcher">
    <h2>{{ title }}</h2>
    <ul>
      {% for item in items %}
        <li>
          <strong>{{ item.name }}</strong> - {{ item.price | default: "Free" }}
        </li>
      {% endfor %}
    </ul>
  </div>
  